# 使用说明 · WorkBuddy-VPS

把本项目部署到云端沙箱，得到一个浏览器可访问的中文开发桌面。

本文件告诉你：**说什么话能触发部署、手动怎么敲命令、要注意什么**。

> **仅适用于 WorkBuddy 国际版。**

---

## 一、发起部署：在对话里说什么

项目已开源在 GitHub，**不需要下载到本地、不需要传文件**，把仓库地址发到对话里即可。

**仓库地址**

```
https://github.com/xl998800111-sketch/WorkBuddy-VPS.git
```

### 最简说法

> 帮我部署这个项目：
> https://github.com/xl998800111-sketch/WorkBuddy-VPS.git

### 推荐说法（把参数一次给全，少来回）

> 帮我部署这个仓库到云端沙箱：
> https://github.com/xl998800111-sketch/WorkBuddy-VPS.git
>
> 克隆后发布整个目录，参数如下：
> - language：`python`
> - startCmd：`sed -i 's/\r$//' deploy.sh; exec bash deploy.sh`
> - appName：`VPS 开发桌面`

### 发出之后会发生什么

1. 从 GitHub 克隆仓库
2. 把整个目录发布到云端沙箱
3. 沙箱内自动执行 `deploy.sh`：生成随机密码 → 改写端口映射 → 拉镜像起容器 → 启动对外代理
4. 返回公网访问地址和登录密码

你只需要等 3-4 分钟（首次要拉 9GB 镜像），期间打开链接能看到实时进度。

### 部署完成后追问

> 部署好了吗？把访问地址和登录密码给我。

### 想先确认沙箱环境行不行

> 先跑一下 `deploy.sh --diag`，看这个沙箱能不能跑 Docker。

### 如果只想用本地目录部署

不想走仓库、直接用手上的文件夹也可以：

> 把这个文件夹发布到云端沙箱，用里面的 deploy.sh：
> `C:\Users\Administrator\桌面\github`

两者效果完全一样。区别只是：**从仓库部署**时会自动去拉代码，**从本地目录部署**时用你机器上现有的文件。

---

## 二、手动命令行部署

不走发布流程，自己在终端里一步步敲。四种场景按需选一种。

### 方式 A：云端沙箱里手动部署（等价于发布流程）

沙箱唯一对外端口是环境变量 `$PORT`（实测 3000），而 workbuddy-vps 默认也占 3000 —— 必须错开。手动做就是：

```bash
# 1. 取代码
cd /workspace
git clone https://github.com/xl998800111-sketch/WorkBuddy-VPS.git
cd WorkBuddy-VPS

# 2. 探测环境（确认 Docker / 权限 / 磁盘 / 网络都可用）
bash deploy.sh --diag

# 3. 一键部署：后台拉镜像起容器 + 前台常驻代理
bash deploy.sh
```

> **关于 `/workspace`**：这是沙箱自带的目录，可写，而且被 `docker-compose.yml` 挂进容器（桌面里也能看到 `/workspace`），所以拿它当落脚点最省事。
>
> 但**项目放在哪都行** —— `deploy.sh` 以「脚本自己所在的目录」为项目根，不依赖 `/workspace`。只有两样东西固定写在 `/workspace` 下：部署日志 `devtop-deploy.log` 和代理脚本 `devtop-proxy.py`。
>
> `bash deploy.sh` 会**常驻在前台**，因为它要占住沙箱唯一的对外端口。
> 不要加 `&` 丢到后台，也不要在同一个终端里再干别的。

### 方式 B：只要容器，不要代理

沙箱不是单端口限制，或者只想验证容器能不能起来：

```bash
cd /workspace/WorkBuddy-VPS
bash deploy.sh --setup      # 只部署，不起代理，跑完即退出
docker compose ps           # 看容器状态
```

### 方式 C：完全手工，一条条敲（不走发布流程）

```bash
cd /workspace/WorkBuddy-VPS

# 1. 生成 20 位随机密码
head -c 4096 /dev/urandom | tr -dc 'A-Za-z0-9' | head -c 20 > /tmp/pwd
printf 'CUSTOM_USER=admin\nPASSWORD=%s\n' "$(cat /tmp/pwd)" > .env
chmod 600 .env

# 2. 改写端口映射：容器内仍是 3000，映射到宿主机 8080，避开沙箱的 $PORT
sed -i "s|0.0.0.0:3000:3000|0.0.0.0:8080:3000|" docker-compose.yml
sed -i "s|0.0.0.0:3001:3001|0.0.0.0:8081:3001|" docker-compose.yml
grep -A 3 'ports:' docker-compose.yml     # 确认改对了

# 3. 拉镜像起容器（约 9GB，首次 3-4 分钟）
docker compose up -d
docker compose ps

# 4. 把沙箱唯一对外端口转发到 8080
#    桌面基于 KasmVNC，依赖 WebSocket，必须走 TCP 层转发，不能用 HTTP 反代
socat TCP-LISTEN:${PORT:-3000},fork,reuseaddr TCP:127.0.0.1:8080
```

> `socat` 没装的话：`apt-get update && apt-get install -y socat`。
>
> 不想装 `socat` 也可以 —— `deploy.sh` 内置了等价的纯 Python 代理。跑一次 `bash deploy.sh`，它会在 `/workspace/devtop-proxy.py` 生成该代理并常驻运行，效果一样。

### 方式 D：本地 Linux 机器（有 Docker，无单端口限制）

沙箱那套端口戏法在这里不需要：

```bash
git clone https://github.com/xl998800111-sketch/WorkBuddy-VPS.git
cd WorkBuddy-VPS

head -c 4096 /dev/urandom | tr -dc 'A-Za-z0-9' | head -c 20 > /tmp/pwd
printf 'CUSTOM_USER=admin\nPASSWORD=%s\n' "$(cat /tmp/pwd)" > .env

docker compose up -d
# 打开 http://<机器IP>:3000
```

### 手动部署的四条硬约束

1. **`$PORT` 必须有人监听**。不跑代理就没人占端口，沙箱判定服务未启动，链接打不开。
2. **代理要在容器之前或同时起来**。发布方的健康检查只等 60 秒，而镜像要拉 3-4 分钟 —— 等容器就绪再起代理必然超时。`deploy.sh` 的做法是先起代理，容器没就绪时它返回"正在部署"页。
3. **端口映射改完要重新 `docker compose up -d` 才生效**。
4. **容器名固定为 `workbuddy-vps`**。改名会同时影响 `docker compose` 的运维命令和旧沙箱里的残留容器。

### 手动部署排障

```bash
docker compose ps                              # 容器起没起
docker compose logs --since 10m workbuddy-vps  # 容器日志（最近 10 分钟）
less /workspace/devtop-deploy.log              # 部署日志（按 G 跳到末尾）
ss -tlnp | grep -E ':3000|:8080'               # 端口到底有没有人在听
```

---

## 三、命令清单

### 发布参数（调用发布能力时使用）

| 参数 | 值 | 说明 |
|---|---|---|
| `directory` | 项目目录绝对路径 | 必填，指向放 deploy.sh 的目录 |
| `language` | `python` | 脚本用 python3 运行代理 |
| `startCmd` | `sed -i 's/\r$//' deploy.sh; exec bash deploy.sh` | **那个 sed 不能省** |
| `appName` | 任意，如 `VPS 开发桌面` | 应用显示名 |

### 沙箱内自动执行

发布后沙箱会自动跑 `startCmd`，也就是：

```bash
bash deploy.sh
```

它会依次完成：环境探测 → 定位项目 → 生成随机密码 → 改写端口映射 → 后台拉镜像起容器 → 启动对外代理。

完成后会把访问地址、用户名、密码和运维命令汇总写到 `/workspace/deploy-info.txt`，随时可以查看：

```bash
cat /workspace/deploy-info.txt
```

### 手动模式

```bash
bash deploy.sh --diag      # 只探测环境，不部署
bash deploy.sh --status    # 查看当前部署状态
bash deploy.sh --setup     # 只部署，不起代理
```

### 运维（在项目目录下执行）

```bash
docker compose ps                             # 状态
docker compose logs -f workbuddy-vps          # 实时日志
docker compose restart workbuddy-vps          # 重启
docker compose down                           # 停止
docker compose up -d                          # 启动
docker compose pull && docker compose up -d   # 更新镜像
docker exec -it workbuddy-vps /bin/bash       # 进容器
```

### 改密码

```bash
# 编辑 .env 里的 PASSWORD=，然后
docker compose down && docker compose up -d
```

---

## 四、注意事项

### 必看四条

1. **不要在本地电脑执行 deploy.sh**。本机没有 Docker 和 `/workspace`，跑了没意义，它只在沙箱里工作。
2. **`startCmd` 里的 sed 不能删**。Windows 写出的脚本是 CRLF 换行，不转换会报 `\r: command not found`，部署直接失败。
3. **首次部署要等 3-4 分钟**。镜像有 9GB，要拉取加解压。期间打开页面会看到实时进度和已生成的凭据，这是正常的。
4. **文件一个都不能少**。尤其 `devtop-init-fix.sh` 和 `assets/config.toml` —— 它们被 `docker-compose.yml` 以相对路径挂载，缺任意一个容器都起不来。

### 遇到问题怎么办

| 现象 | 原因与处理 |
|---|---|
| 页面一直显示"正在部署" | 正常，镜像还没拉完，等 |
| 网关报 `500 socket hang up` | 容器刚起、nginx 未就绪，等 30 秒重试 |
| 发布报 `port 3000 NOT listening` | `startCmd` 不完整，或 `deploy.sh` 是 CRLF 换行 |
| 报 `\r: command not found` | 同上，换行符问题 |
| 打开打不开、等一会才行 | 沙箱休眠了，重新访问会自动唤醒，约 30 秒 |
| 要求输入用户名密码 | 这是 HTTP Basic 认证，属预期保护，凭据见部署页面或 `.env` |
| 输入法用不了 | 桌面终端执行 `fcitx5 -d --replace` |
| 想知道沙箱内部状态 | 访问 `<访问地址>/__diag`，端口、容器、日志一目了然 |

### 安全五条

1. **公开链接等于全网可见**。本项目用 HTTP Basic 认证做了保护，请务必保留随机密码，不要改成弱口令。
2. **不要把密钥、生产数据、隐私文件放进桌面**。
3. **不要往部署目录里放 `.env` 或密钥文件**，它们会被一起上传到云端。
4. **链接不是永久的**。沙箱闲置可能被回收，不适合当生产环境长期依赖。
5. **沙箱数据随沙箱生命周期存在**，要留存的东西及时导出。

### 重新发布注意

- 同一个目录重新发布会**复用原链接**，线上内容被直接替换。如果链接已经分享给别人，注意这一点。
- 如果改过容器名（比如从 `workbuddy-devtop` 改成 `workbuddy-vps`），旧沙箱里还留着旧容器占着 8080 端口，需要先清掉：

  ```bash
  docker rm -f workbuddy-devtop
  ```

  在**全新沙箱**里部署不存在这个问题。

---

## 五、部署成功的判断标准

部署完成后，打开访问地址应该看到：

| 检查项 | 预期 |
|---|---|
| 不带凭据访问 | 返回 `401`，弹认证框 |
| 填入用户名密码 | 返回 `200`，进入桌面 |
| 访问 `/__diag` | 能看到端口监听、容器状态、日志 |

只要这三条对上，就是部署成功了。用户名固定为 `admin`，密码是脚本随机生成的 20 位字符。
